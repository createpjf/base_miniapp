/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'via.placeholder.com', 'assets.coingecko.com', 'cryptologos.cc'],
  },
}

module.exports = nextConfig
